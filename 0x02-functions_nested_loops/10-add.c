#include "main.h"
#include <stdio.h>
/**
 * add - adds two integers
 *
 * @q: Integer one
 *
 * @r: Integer two
 *
 * Return: Returns the result
 */
int add(int q, int r)
{
int sum;
sum = q + r;
return (sum);
}
