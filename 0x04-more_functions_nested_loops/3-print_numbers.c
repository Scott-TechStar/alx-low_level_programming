#include "main.h"
#include <stdio.h>
/**
 * print_numbers - print numbers 
 *
 * Return: null 
 */
void print_numbers(void)
{
int i;  
if ((c >= 48) && (c <= 57))
{
for (i = 48; i < 57; i++)
{
_putchar(i);
}
}
_putchar('\n');
}
