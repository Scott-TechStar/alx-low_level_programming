#include "main.h"
#include <stdio.h>
/**
 * mul - multiplies two integers
 *
 * @a: Integer one
 *
 * @b: Integer two
 *
 * Return: Returns the result
 */
int mul(int a, int b)
{
int mul;
mul = a * b;
return (mul);
}
